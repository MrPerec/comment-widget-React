`use strict`;

const BOX_WIDTH = 48;
const FIELD_WIDTH = 50;
const FIELD_HEIGHT = 5;
const TIME_LOCALE = `ru`;
const TIME_OPTIONS = {day: `numeric`, month: `numeric`, year: `numeric`};
const STORAGE_COMMENTS = `oldComments`;

export {BOX_WIDTH, FIELD_WIDTH, FIELD_HEIGHT, TIME_LOCALE, TIME_OPTIONS, STORAGE_COMMENTS}