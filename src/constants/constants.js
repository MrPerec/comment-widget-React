`use strict`;

const NUMBER_COMMENTS = 1; 
const BOX_WIDTH = 48;
const FIELD_WIDTH = 50;
const FIELD_HEIGHT = 5;
const TIME_LOCALE = `ru`;
const TIME_OPTIONS = {day: `numeric`, month: `numeric`, year: `numeric`};
const STORAGE_OLD_COMMENTS = `oldComments`;

export {NUMBER_COMMENTS, BOX_WIDTH, FIELD_WIDTH, FIELD_HEIGHT, TIME_LOCALE, TIME_OPTIONS, STORAGE_OLD_COMMENTS}
